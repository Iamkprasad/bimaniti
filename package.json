{
  "name": "bimaniti",
  "version": "1.0.0",
  "description": "BimaNiti — Insurance & Market Analysis platform (static site)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "minify": "node scripts/minify.mjs",
    "prebuild": "npm run minify"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
